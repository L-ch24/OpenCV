# 해당 코드는 Google Colab에서 실행을 바탕으로 제작함
# OpenCV 설치
!pip install opencv-python

# 라이브러리 불러오기
import cv2
from google.colab.patches import cv2_imshow

# 이미지 읽기
image = cv2.imread("/content/apple.jpg")

# 이미지 출력
if image is None:
    print("이미지를 찾을 수 없습니다.")
else:
    cv2_imshow(image)

# 33ms 대기
cv2.waitKey(33)
